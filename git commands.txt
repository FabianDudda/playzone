
# Neu hinzugefügte Dateien löschen	
git clean -fd

# Reset auf letzten Commit	
git reset --hard HEAD


------------

# Änderungen zur Staging-Area hinzufügen:
git add .

# oder nur eine bestimmte Datei:
git add dateiname.txt

# Commit erstellen:
git commit -m "Kurze, prägnante Commit-Nachricht"

# Änderungen pushen:
git push

------------------------------

# stage, commit, push to dev 
git add . && git commit -m "commit note" && git push origin dev

# then merge into main  
git checkout main && git merge dev && git push origin main && git checkout dev 

------------------------------

give me a concept for this:
i want to implement a list view of the places. 
for that im thinking of a minimized bottom sheet on the / above the map with only showing the title "36 Sportplätze gefunden" (number of places found in the current map section).
when user swipe it up or tap it, the sheet should expand to 50% of height showing a list of places available in the current map section. for list design use for now the same ui design as in favorite-sheet.
think about how to handle performance for getting the number of places in map section in title and the list items in sheet. maybe only load the first 10 or so and when user browse through the list and 
is on the end of list, load more places?

---
